# Demo capture

The README demo asset lives at `docs/assets/knarr-react-vite-pnpm-demo.gif`.

When replacing it with a real screen capture, use the React + Vite example app and show this exact flow:

```bash
cd my-app
npx knarr use ../my-lib

cd ../my-lib
knarr dev
```

Recommended shot list:

1. Consumer app running in the browser
2. Terminal showing `knarr use ../my-lib`
3. Library source edit
4. `knarr dev` publishing and pushing
5. Consumer app updating without a package.json diff

Keep the final GIF under 2 MB if possible so the README stays quick to load.
