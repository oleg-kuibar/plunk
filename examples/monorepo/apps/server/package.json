{
  "name": "@mono/server",
  "version": "1.0.0",
  "private": true,
  "description": "Node.js app — uses shared-utils (workspace) + ui-kit (Knarr)",
  "type": "module",
  "scripts": {
    "start": "node --import tsx src/main.ts",
    "postinstall": "knarr restore || true"
  },
  "dependencies": {
    "@mono/shared-utils": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
